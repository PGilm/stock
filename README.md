# Stock Performance Dashboard

A simple Streamlit app showing the normalized returns of different indexes, funds or stocks including some risk metrics.

[![Open in Streamlit]

### How to run it on your own machine

1. Install the requirements

   ```
   $ pip install -r requirements.txt
   ```

2. Run the app

   ```
   $ streamlit run pgStocks.py
   ```
